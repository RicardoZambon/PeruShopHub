# Ralph Progress Log
Started: Fri Mar 28 2026
---

## Codebase Patterns
- All components are **standalone** (no NgModules) with `standalone: true`
- Use `CommonModule` import for `*ngFor`, `*ngIf` directives
- Barrel exports in `shared/components/index.ts` — export both component and types
- SCSS uses `@use '../../../../styles/mixins' as m;` for responsive breakpoint mixins
- CSS custom properties from `tokens.css` for all design tokens (e.g., `var(--surface)`, `var(--neutral-200)`)
- IntersectionObserver for infinite scroll: run outside NgZone, use scroll container as root, re-enter zone only to emit events
- Angular build via `npx ng build --configuration=development` — takes ~95 seconds
- Template file convention: separate `.html` file with `templateUrl`
- Style file convention: separate `.scss` file with `styleUrl` (singular, not `styleUrls`)
- **Service layer pattern**: All business logic in Application/Services, controllers are thin HTTP adapters
- **Exception pattern**: NotFoundException (404), AppValidationException (400 with errors dict), ConflictException (409) — caught by GlobalExceptionFilter
- **Validation pattern**: Collect all errors into `Dictionary<string, List<string>>` before throwing AppValidationException
- **Optimistic locking**: Version property + ConcurrencyToken on Product, Category, PurchaseOrder, Supply — set OriginalValue, increment, catch DbUpdateConcurrencyException
- **Role-based auth**: Admin for Settings, Admin/Manager for mutations, any authenticated for reads
- **Build command**: `dotnet build src/PeruShopHub.API/PeruShopHub.API.csproj /nodeReuse:false /p:UseSharedCompilation=false -o /tmp/perushophub-build` (VS Code locks default bin/)
- Application project needs explicit EF Core 9.* package reference due to version resolution issues
- BCrypt.Net-Next already referenced in Infrastructure project
- Product interface defined inline in `product.service.ts`, Category in `models/category.model.ts`, Supply in `supply.service.ts`, PO in `purchase-order.service.ts`
- Error interceptor uses functional `HttpInterceptorFn` pattern (not class-based)
- Remote is `RicardoZambon/PeruShopHub` on GitHub — use for badge URLs and ghcr.io paths
- **Test infrastructure**: Unit tests in `tests/PeruShopHub.UnitTests/` (xUnit + FluentAssertions + Moq), integration tests in `tests/PeruShopHub.IntegrationTests/` (xUnit + TestContainers)
- **Integration test pattern**: Use `[Collection("Integration")]` attribute + `IntegrationTestBase` base class. `CustomWebApplicationFactory` provides PostgreSQL 16 + Redis 7 via TestContainers, auto-migrates DB
- **Program class accessibility**: `public partial class Program { }` at end of `Program.cs` enables `WebApplicationFactory<Program>` in tests
- **TestContainers builder pattern**: Use `new PostgreSqlBuilder("postgres:16-alpine")` (image in constructor, not `.WithImage()`)
- **Unit test DB pattern**: Use SQLite in-memory (`SqliteConnection("DataSource=:memory:")`) with `EnsureCreated()` for unit tests. Provide `ITenantContext` mock with `IsSuperAdmin=true` to bypass query filters
- **SQLite + EF Core gotcha**: SQLite DELETE in batch with INSERT/UPDATE on same table causes `DbUpdateConcurrencyException`. Workaround: test delete logic separately or test the underlying calculation methods directly
- **CostCalculationService testing**: Service uses DbContext directly (not repository), so tests need real DB (SQLite in-memory). Mock `INotificationDispatcher` and `ILogger`. Use `IConfiguration` in-memory collection for tax rate

---

## 2026-03-28 - US-001
- Created `.github/workflows/ci.yml` with parallel backend and frontend jobs
- Backend job: dotnet restore → build → test (Release config)
- Frontend job: npm ci → ng build --configuration=production → ng test --watch=false --browsers=ChromeHeadless
- Triggers on push to main and all PRs
- Created `README.md` with CI status badge
- Files changed: `.github/workflows/ci.yml`, `README.md`
- **Learnings for future iterations:**
  - .NET build has 4 MSB3277 warnings (EF Core version resolution between Infrastructure/Application) — not blocking but worth cleaning up later
  - No test projects exist yet in the solution — `dotnet test` will pass vacuously until US-003 adds them
  - Solution file is at repo root: `PeruShopHub.sln`
  - Node 20 is a reasonable default for Angular 17+ CI
---

## 2026-03-28 - US-002
- Created `.github/workflows/docker.yml` with matrix strategy for all 3 images (api, worker, web)
- Triggers only on push to `main` (not on PRs)
- Builds `Dockerfile.api`, `Dockerfile.worker`, `Dockerfile.web` using matrix strategy
- Pushes to ghcr.io with commit SHA and `latest` tags
- Uses GitHub Actions cache (`type=gha`) for Docker layer caching via Buildx
- Uses `docker/metadata-action@v5` for tag/label generation
- .NET build passes (warnings only, 0 errors)
- Files changed: `.github/workflows/docker.yml`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Docker workflow uses matrix strategy to build all 3 images in parallel — efficient and DRY
  - `docker/build-push-action@v6` with `cache-from: type=gha` + `cache-to: type=gha,mode=max` is the standard GHA caching approach
  - Dockerfiles use repo root as build context (`.`) since they reference `src/` and `docker/` paths
  - `GITHUB_TOKEN` has built-in write access to ghcr.io when `permissions.packages: write` is set
---

## 2026-03-28 - US-003
- Created `tests/PeruShopHub.UnitTests/` project with xUnit 2.x, FluentAssertions 8.x, Moq 4.x, coverlet
- Created `tests/PeruShopHub.IntegrationTests/` project with xUnit 2.x, FluentAssertions 8.x, Testcontainers.PostgreSql 4.x, Testcontainers.Redis 4.x, Microsoft.AspNetCore.Mvc.Testing 9.x
- Created `IntegrationTestBase` class providing HttpClient, scoped DbContext access, shared collection fixture
- Created `CustomWebApplicationFactory<Program>` that spins up PostgreSQL 16 + Redis 7 via TestContainers, replaces DB/cache/SignalR registrations, auto-migrates database
- Created `IntegrationTestCollection` for shared fixture across all integration tests
- Added `public partial class Program { }` to API's Program.cs for WebApplicationFactory access
- Added both projects to `PeruShopHub.sln`
- Updated CI pipeline to run Unit Tests and Integration Tests as separate steps
- Smoke tests: UnitTests (1 test passing), IntegrationTests (2 tests — health endpoint + DB migration, require Docker)
- .NET build passes (0 errors, 5 pre-existing warnings)
- Files changed: `tests/PeruShopHub.UnitTests/**`, `tests/PeruShopHub.IntegrationTests/**`, `src/PeruShopHub.API/Program.cs`, `.github/workflows/ci.yml`, `PeruShopHub.sln`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - TestContainers 4.x uses image string in constructor: `new PostgreSqlBuilder("postgres:16-alpine")`, not `new PostgreSqlBuilder().WithImage(...)`
  - `WebApplicationFactory<Program>` requires Program class to be accessible — add `public partial class Program { }` at end of top-level statement file
  - SignalR Redis backplane services are hard to cleanly remove from DI — simplest approach is to remove Redis-related SignalR descriptors and re-add plain SignalR
  - Integration tests can't run without Docker — CI on `ubuntu-latest` has Docker, local dev may not
  - xUnit collection fixtures (`ICollectionFixture<T>`) share a single instance across all tests in the collection — ideal for TestContainers to avoid spinning up containers per test class
---

## 2026-03-28 - US-004
- Created 33 unit tests for `CostCalculationService` covering all financial calculation logic
- Tests organized in `tests/PeruShopHub.UnitTests/Services/CostCalculationServiceTests.cs`
- Added `Microsoft.EntityFrameworkCore.Sqlite` package for in-memory database testing
- Test categories:
  - `CalculateOrderCostsAsync`: product_cost (3 tests), packaging (1), commission resolution (4: specific, category fallback, default fallback, hardcoded 13%), fixed_fee brackets (10 Theory cases + 1 quantity multiplier), tax (2: configured + default 6%), all categories returned (1), profit calculation (1)
  - `RecalculateOrderCostsAsync`: preserves manual costs while refreshing calculated costs (1)
  - `ReceivePurchaseOrderAsync`: weighted average cost (2: existing stock + zero stock), cost distribution by_value (1), cost distribution by_quantity (1), status gating (1), creates history + stock movements (1), zero quantity edge case (1), product-level cost update (1)
  - Edge cases: zero price order (1)
- All 34 tests pass (33 new + 1 pre-existing smoke test), .NET build passes (0 errors)
- Files changed: `tests/PeruShopHub.UnitTests/Services/CostCalculationServiceTests.cs` (new), `tests/PeruShopHub.UnitTests/PeruShopHub.UnitTests.csproj` (added SQLite package), `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - SQLite in-memory requires keeping the `SqliteConnection` open for the test lifetime — use `IDisposable` to clean up
  - Must provide `ITenantContext` mock with `IsSuperAdmin=true` when constructing `PeruShopHubDbContext` for tests — otherwise the tenant query filter causes NullReferenceException in queries
  - SQLite + EF Core has issues with batch DELETE+INSERT on the same table — causes `DbUpdateConcurrencyException`. When testing methods that delete and re-add entities (like `RecalculateOrderCostsAsync`), test the underlying calculation logic separately
  - `ChangeTracker.Clear()` is essential between setup and test execution to avoid stale tracking state
  - `IConfiguration.GetValue<decimal>()` with in-memory collection works well for configuring tax rate in tests
  - Commission resolution tests need careful setup of `CommissionRule` entities with correct MarketplaceId, CategoryPattern, and ListingType combinations
---

## 2026-03-28 - US-005a
- Created 22 unit tests for ProductService and 27 unit tests for OrderService (49 total, exceeding minimum 12)
- ProductService tests cover: create (with auto-SKU generation, SKU increment), create validation (duplicate SKU, empty name, negative costs, invalid category), notification broadcast, GetById (found + not found), Update (valid + not found), Delete (hard delete without orders, soft delete with orders, not found), GetList (pagination, search filter, status filter, category filter with descendants), GetNextSku (with prefix, null category, no prefix)
- OrderService tests cover: GetList (pagination, search by buyer name, search by external ID, status filter, date range filter), GetById (financials calculation, timeline building for Pago/Cancelado/Entregue statuses, payment status derivation for 6 statuses), AddCost (valid + not found + validation), UpdateCost (valid + not found), DeleteCost (found + not found), Fulfill (delegates to cost service), RecalculateCosts (valid + not found)
- Files changed: `tests/PeruShopHub.UnitTests/Services/ProductServiceTests.cs` (new), `tests/PeruShopHub.UnitTests/Services/OrderServiceTests.cs` (new), `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - ProductService uses ICacheService for list caching — mock returns null to bypass cache in tests
  - ProductService.DeleteAsync does soft delete (IsActive=false, Status="Excluído") when OrderItems reference the product, hard delete otherwise
  - OrderService.GetByIdAsync calculates financials inline (revenue - totalCosts = profit, profit/revenue*100 = margin) — not stored on entity
  - OrderService timeline and payment status are derived from order status using static helper methods BuildTimeline and DerivePaymentStatus
  - OrderService.FulfillAsync and RecalculateCostsAsync delegate to ICostCalculationService — mock verification is sufficient
  - SQLite in-memory DB works well for testing both services since they use DbContext directly
---
