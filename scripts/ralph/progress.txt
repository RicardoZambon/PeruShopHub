# Ralph Progress Log
Started: Wed Mar 25 04:14:54 AM UTC 2026
---

## Codebase Patterns
- All components are **standalone** (no NgModules) with `standalone: true`
- Use `CommonModule` import for `*ngFor`, `*ngIf` directives
- Barrel exports in `shared/components/index.ts` — export both component and types
- SCSS uses `@use '../../../../styles/mixins' as m;` for responsive breakpoint mixins
- CSS custom properties from `tokens.css` for all design tokens (e.g., `var(--surface)`, `var(--neutral-200)`)
- Existing data-table uses BEM naming: `.data-table__scroll-wrapper`, `.data-table__table`, etc.
- IntersectionObserver for infinite scroll: run outside NgZone, use scroll container as root, re-enter zone only to emit events
- Angular build via `npx ng build --configuration=development` — takes ~95 seconds
- Use `EventEmitter.observed` to conditionally apply behavior only when an output has subscribers
- Migration pattern for list pages: use `loadProducts(reset: boolean)` — reset=true clears data + resets page, reset=false appends for infinite scroll
- GridColumn keys should match property names on the data objects (no rename mapping needed)
- Template file convention: separate `.html` file with `templateUrl`
- Style file convention: separate `.scss` file with `styleUrl` (singular, not `styleUrls`)

---

## 2026-03-25 04:20 UTC - US-001
- Created `DataGridComponent` in `shared/components/data-grid/`
- GridColumn interface with: key, label, align, width, sortable, sticky, headerClass, cellClass
- Renders `<table>` with `<thead>` and `<tbody>` from column definitions
- Default cell rendering: `row[column.key]` as text
- Columns with `align` apply to both `<th>` and `<td>`
- Columns with `width` set apply via `[style.width]`
- OnPush change detection strategy
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`, `shared/components/index.ts`
- **Learnings for future iterations:**
  - The existing `data-table` component uses inline template; new `data-grid` uses separate `.html` file for maintainability as it will grow
  - `styleUrl` (singular) is the correct property name in Angular 21, not `styleUrls`
  - Build typecheck confirms correctness; `ng build` is the reliable check
---

## 2026-03-25 04:26 UTC - US-002
- Created `GridCellDirective` with `appGridCell` selector keyed by column name
- Grid uses `@ContentChildren(GridCellDirective)` to collect custom templates
- `getCellTemplate(columnKey)` method looks up the matching directive's templateRef
- Template context provides `$implicit` (full row) and `value` (cell value) — usage: `<ng-template appGridCell="colKey" let-row let-value="value">`
- When no custom template registered, falls back to default text rendering
- Exported `GridCellDirective` and `GridCellContext` from barrel index
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `shared/components/index.ts`
- **Learnings for future iterations:**
  - `@ContentChildren` requires the directive to be `standalone: true` to work with standalone host components
  - Template context uses `$implicit` for `let-row` (no assignment) and named keys for `let-value="value"`
  - `getCellTemplate()` is called per cell per render — acceptable for OnPush since it only runs on change detection
---

## 2026-03-25 04:28 UTC - US-003
- Created `GridHeaderDirective` with `appGridHeader` selector keyed by column name
- Grid uses `@ContentChildren(GridHeaderDirective)` to collect header templates
- `getHeaderTemplate(columnKey)` looks up matching directive's templateRef
- Template replaces default `{{ col.label }}` text when registered; falls back to label otherwise
- Exported `GridHeaderDirective` from barrel index
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `shared/components/index.ts`
- **Learnings for future iterations:**
  - Pattern identical to GridCellDirective — new content directives follow same structure: `@Directive` + `@Input` for key + `TemplateRef` injection
  - Header templates use `TemplateRef<void>` since no row context needed (unlike cell templates)
---

## 2026-03-25 04:30 UTC - US-004
- Added server-side sorting support to DataGridComponent
- `sortChange` output emits `GridSortEvent` with `{ column, direction }`
- `activeSort` tracks current sort state; `onSortClick()` cycles asc → desc → null
- `getSortDirection()` helper for template to query current column sort state
- Inline SVG sort indicators (dual-arrow neutral, highlighted arrow for asc/desc)
- Sortable headers get `cursor: pointer` + hover highlight; active sort column uses `--primary` color
- Exported `GridSortEvent` and `SortDirection` types from barrel index
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`, `shared/components/index.ts`
- **Learnings for future iterations:**
  - Inline SVGs are simpler than icon fonts for small indicators — no extra dependencies
  - The sort cycle pattern (asc → desc → null) resets column to empty string when direction is null, so `getSortDirection` comparison works cleanly
  - Build takes ~20 seconds with development config — fast enough for iterative checks
---

## 2026-03-25 04:33 UTC - US-005
- Added infinite scroll to DataGridComponent using IntersectionObserver on a sentinel element
- New inputs: `loading` (boolean), `hasMore` (boolean), `pageSize` (default 20)
- New output: `loadMore` event emitted when sentinel enters viewport and `hasMore && !loading`
- Observer runs outside NgZone for performance, re-enters zone only to emit
- Loading spinner row shown at bottom when `loading && data.length > 0`
- Scroll wrapper already had `overflow: auto` and `flex: 1` from US-001 — scroll position naturally preserved when rows appended
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - IntersectionObserver root must be the scroll container (`.data-grid__scroll-wrapper`), found via `closest()` from sentinel
  - Running observer outside NgZone prevents unnecessary change detection on scroll
  - Sentinel is a 1px div after the table — always at the bottom of scroll content
  - `AfterViewInit` is needed because `@ViewChild` with `static: false` isn't available in `OnInit`
---

## 2026-03-25 04:35 UTC - US-006
- Added initial loading state with skeleton placeholders to DataGridComponent
- `showSkeleton` computed property: `loading && data.length === 0`
- Skeleton rows (default 8, configurable via `skeletonRows` input) mirror column layout with same alignment and widths
- Uses existing `<app-skeleton>` component with `type="text"` and 16px height
- Right-aligned columns get 60% width skeletons with `margin-left: auto`, others get 80%
- When `loading && data.length > 0`, skeleton NOT shown (existing bottom spinner handles this)
- Imported `SkeletonComponent` into DataGridComponent standalone imports
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - `app-skeleton` needs `display: block` on the host to respect margin-left for right-aligned cells
  - Skeleton rows reuse the same `<td>` structure as data rows for consistent column widths
  - `showSkeleton` as a getter is fine for OnPush — only evaluated on change detection
---

## 2026-03-25 04:37 UTC - US-007
- Added empty state support to DataGridComponent
- Created `GridEmptyDirective` with `appGridEmpty` selector for custom empty state content
- Added `emptyTitle` and `emptyDescription` inputs for default empty state
- `showEmpty` getter: `!loading && data.length === 0`
- Default empty state renders an SVG table icon, title, and optional description
- Empty state vertically centered via flexbox with `min-height: 200px`
- Custom template via `<ng-template appGridEmpty>` replaces default when provided
- Exported `GridEmptyDirective` from barrel index
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`, `shared/components/index.ts`
- **Learnings for future iterations:**
  - Content projection directives follow consistent pattern: `@Directive` + `TemplateRef` injection + `@ContentChildren` query in host
  - `GridEmptyDirective` uses `TemplateRef<void>` like `GridHeaderDirective` (no context needed)
  - Empty state uses `flex: 1` to fill available space within the scroll wrapper for vertical centering
---

## 2026-03-25 04:40 UTC - US-008
- Added mobile card layout to DataGridComponent
- Created `GridCardDirective` with `appGridCard` selector for custom mobile card templates
- Table hidden below 768px via `display: none`, card list shown via `display: flex`
- Default card renders column label-value pairs stacked vertically with separator borders
- Custom cell templates reused in default card layout (same `getCellTemplate` lookup)
- Custom card template via `<ng-template appGridCard let-row>` replaces default when provided
- Skeleton cards shown during initial loading on mobile (matching card structure)
- Infinite scroll works on cards (same sentinel + IntersectionObserver in scroll wrapper)
- Exported `GridCardDirective` and `GridCardContext` from barrel index
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`, `shared/components/index.ts`
- **Learnings for future iterations:**
  - Use `display: table` / `display: none` toggle for table show/hide (not `visibility`) since `<table>` needs `display: table` not `display: block`
  - The `@include m.mobile` mixin uses `max-width: 767px` — consistent with project's 768px tablet breakpoint
  - Cards reuse the same cell templates as the table, so custom cell rendering works on both desktop and mobile automatically
  - Infinite scroll sentinel is shared between table and card views since it's outside both containers
---

## 2026-03-25 04:42 UTC - US-009
- Added `rowClick` output that emits full row object on row/card click
- `onRowClick()` method filters out clicks on interactive elements (`button`, `a`, `input`, `select`, `textarea`, `[role="button"]`) using `closest()`
- `hasRowClick` getter uses `EventEmitter.observed` to conditionally apply clickable class only when parent listens
- Desktop rows get `data-grid__row--clickable` class with `cursor: pointer` and hover highlight
- Mobile cards get `data-grid__card--clickable` class with same cursor and hover styling
- Both custom and default card templates get click handlers
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - `EventEmitter.observed` (boolean) is useful to conditionally apply styles only when the output has a subscriber — avoids unnecessary cursor/hover on grids that don't use row clicks
  - `element.closest()` is the cleanest way to check if a click originated from an interactive element — works regardless of nesting depth
  - Hover highlight on table rows uses `var(--neutral-50)` which is the same as the header background — subtle but visible
---

## 2026-03-25 04:45 UTC - US-010
- Added sticky column support for columns marked `sticky: true`
- Sticky cells use `position: sticky; left: 0` with `::after` pseudo-element for right border shadow
- z-index layering: sticky header cells (z-index: 3) > sticky header row (z-index: 2) > sticky body cells (z-index: 1)
- Sticky cells in `<thead>` use `var(--neutral-50)` background to match header; body cells use `var(--surface)`
- Clickable row hover also updates sticky cell background to prevent visual gap
- Added custom scrollbar styling on scroll wrapper: `scrollbar-width: thin`, webkit overrides matching global app design
- Sticky class applied to `<th>`, skeleton `<td>`, and data `<td>` via `[class.data-grid__cell--sticky]="col.sticky"`
- Files changed: `data-grid.component.html`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - Sticky header + sticky column intersection needs z-index: 3 (higher than both) so the corner cell stays on top
  - `::after` pseudo-element with gradient shadow is the standard pattern for indicating scroll underneath a sticky element
  - Sticky cells need explicit background color because `position: sticky` makes content scroll behind transparent cells
---

## 2026-03-25 04:47 UTC - US-011
- Added public `scrollToTop()` method to DataGridComponent
- Added `#scrollWrapper` template ref and `@ViewChild('scrollWrapper')` to access scroll container
- Added `scroll-behavior: smooth` CSS to scroll wrapper for smooth animation
- `scrollToTop()` uses `scrollTo({ top: 0 })` — smooth behavior handled by CSS
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - `scroll-behavior: smooth` on the CSS is cleaner than passing `behavior: 'smooth'` in JS — it applies to all programmatic scrolls on that container
  - The scroll wrapper `#scrollWrapper` ViewChild is also useful for future `scrollToRow()` (US-012)
---

## 2026-03-25 04:48 UTC - US-012
- Added public `scrollToRow(predicate: (row: any) => boolean)` method to DataGridComponent
- Returns `false` if no matching row found in current data (so parent can load more)
- Finds matching DOM elements in both desktop table rows and mobile card elements via querySelectorAll
- Uses `scrollIntoView({ block: 'center' })` to bring matching element into view
- Applies `data-grid__highlight` class with 1.5s fade pulse animation, removed via setTimeout
- Animation uses `@keyframes data-grid-highlight-pulse` transitioning from primary-light color to transparent
- Files changed: `data-grid.component.ts`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - `scrollIntoView` with `block: 'center'` gives a better UX than `block: 'nearest'` for deep linking — the row appears in the middle of the viewport
  - Using CSS class + setTimeout for the highlight is simpler than Angular animations for a one-shot effect
  - querySelectorAll on DOM is acceptable here since `scrollToRow` is called imperatively (not per render cycle)
---

## 2026-03-25 04:50 UTC - US-013
- Added ARIA accessibility attributes to DataGridComponent
- Table gets `role="grid"` and configurable `ariaLabel` input (default 'Data grid')
- Header cells get `role="columnheader"` with `aria-sort` attribute for sortable columns (ascending/descending/none)
- Body cells get `role="gridcell"`
- Loading state announced via `aria-live="polite"` region with screen-reader-only text
- Empty state container gets `aria-live="polite"` so screen readers announce when no data
- Infinite scroll spinner gets `aria-label="Carregando mais itens"` and `role="status"`
- Added `.data-grid__sr-only` CSS class for visually hidden but screen-reader-accessible content
- Files changed: `data-grid.component.ts`, `data-grid.component.html`, `data-grid.component.scss`
- **Learnings for future iterations:**
  - `aria-sort` should only be present on sortable columns (use `null` for non-sortable via `[attr.aria-sort]`)
  - `aria-live="polite"` regions should be always present in DOM (not conditionally rendered) — the content inside changes to trigger announcements
  - Screen-reader-only class uses the standard clip/overflow hidden technique — keeps element in accessibility tree but invisible
---

## 2026-03-25 04:57 UTC - US-014
- Migrated Products list page from custom table HTML to `<app-data-grid>` with all custom cell templates
- Replaced pagination with infinite scroll: `loadProducts(reset)` appends data on `loadMore`, resets on filter/sort change
- Custom cell templates for all 9 columns: imageUrl (thumb), name (truncated), sku (mono), price (BRL currency), stock (conditional coloring), variantCount (badge + review), status (badge), margin (margin-badge), actions (edit button)
- Custom mobile card template via `appGridCard` directive matching original card design
- Removed all old custom table HTML (desktop table, mobile cards, pagination)
- Removed old SCSS: scroll wrapper, th, td, row, pagination styles — kept only product-specific styles (thumb, badges, card layout)
- `gridRef.scrollToTop()` called on search/filter/sort changes
- Files changed: `products-list.component.ts`, `products-list.component.html`, `products-list.component.scss`
- **Learnings for future iterations:**
  - Use `gridData` computed that maps Product to Record<string, any> with keys matching `GridColumn.key` — avoids renaming confusion
  - `loadProducts(reset: boolean)` pattern: reset=true clears data + page, reset=false appends — clean infinite scroll lifecycle
  - Custom cell templates use `let-row let-value="value"` — `value` is `row[col.key]`, `row` is the full object
  - `appGridCard` directive provides custom mobile card; the data-grid wraps it in its card container with click handler
  - `$event.stopPropagation()` still needed on interactive elements inside cell templates (edit button, variant badge click)
---

## 2026-03-25 05:04 UTC - US-015
- Migrated Sales list page from custom table HTML to `<app-data-grid>` with all custom cell templates
- Replaced pagination with infinite scroll: `loadOrders(reset)` appends data on `loadMore`, resets on filter/sort/date change
- Custom cell templates for: id (mono with #prefix), date (formatted), buyer (truncated), total (BRL currency mono), profit (conditional green/red mono), status (badge)
- Custom mobile card template via `appGridCard` directive matching original card design
- Removed all old custom table HTML (desktop table, mobile cards, pagination)
- Removed old SCSS: scroll wrapper, th, td, row, pagination styles — kept only vendas-specific styles (mono, profit/loss colors, card layout)
- Replaced `effect()` reactive pattern with explicit `ngOnInit()` + `loadOrders(true)` on filter/sort/date changes + `gridRef.scrollToTop()`
- Date range filters trigger reload + scrollToTop on change
- Files changed: `sales-list.component.ts`, `sales-list.component.html`, `sales-list.component.scss`
- **Learnings for future iterations:**
  - The `effect()` pattern from original sales-list was replaced with explicit handlers like in products-list — `effect()` causes double-load issues with infinite scroll since signals change during load
  - Sales-list had `onStatusChange(event: Event)` using raw DOM events AND `onStatusFilterChange(value: string)` — only the latter is needed with `SelectDropdownComponent`
  - BEM class renaming from `vendas-table__td--profit` to `vendas__profit` keeps styles consistent when moving from custom table to data-grid
  - `DataTableComponent` import replaced with `DataGridComponent` + directives — remove old import to avoid unused reference warnings
---
